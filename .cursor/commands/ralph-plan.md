# ralph-plan

Using only the model defined above, generate a step-by-step plan in the `prd.json` file, in the root directory. Follow the format of the example in `prd.json.example`. Ensure that all actions respect the defined constraints and state transitions.